<%= form_for(@finalposition) do |f| %>
  <% if @finalposition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finalposition.errors.count, "error") %> prohibited this finalposition from being saved:</h2>

      <ul>
      <% @finalposition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
