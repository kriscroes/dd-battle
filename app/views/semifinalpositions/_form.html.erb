<%= form_for(@semifinalposition) do |f| %>
  <% if @semifinalposition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@semifinalposition.errors.count, "error") %> prohibited this semifinalposition from being saved:</h2>

      <ul>
      <% @semifinalposition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
